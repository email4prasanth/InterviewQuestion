┌─────────────────────────────────────────────────────────────────────────────────────────┐
│                    INCREMENTAL LOAD - SCD TYPE 2 (dim_employee)                          │
└─────────────────────────────────────────────────────────────────────────────────────────┘

         NEW DATA COMING                    EXISTING DELTA TABLE
              │                                    │
              ▼                                    ▼
    ┌─────────────────┐                  ┌─────────────────────────────────┐
    │  New Employee   │                  │  dim_employee (Current State)    │
    │  Records        │                  │                                   │
    │                 │                  │  employee_id │ name │ grade │ is_active │
    │  employee_id:101│                  │  101         │ Raj  │ P3    │ 1         │
    │  grade: P4      │                  │  102         │ Pri  │ P2    │ 1         │
    └────────┬────────┘                  └─────────────────────────────────┘
             │
             ▼
    ┌─────────────────────────────────────────────────────────────────────────┐
    │                        MERGE (UPSERT) OPERATION                         │
    │                                                                          │
    │  MERGE INTO dim_employee AS t                                           │
    │  USING new_employee_data AS s                                           │
    │  ON t.employee_id = s.employee_id                                       │
    │                                                                          │
    │  WHEN MATCHED AND t.grade != s.grade THEN                               │
    │      UPDATE SET is_active = 0,                                          │
    │                 effective_end_date = CURRENT_DATE()                     │
    │                                                                          │
    │  WHEN NOT MATCHED THEN                                                  │
    │      INSERT (employee_id, name, grade, is_active, effective_date)      │
    │      VALUES (s.employee_id, s.name, s.grade, 1, CURRENT_DATE())        │
    └─────────────────────────────────────────────────────────────────────────┘
             │
             ▼
    ┌─────────────────────────────────────────────────────────────────────────┐
    │                     RESULT (SCD Type 2 Table)                            │
    │                                                                          │
    │  employee_id │ name      │ grade │ is_active │ effective_date │ end_date │
    │  ─────────────────────────────────────────────────────────────────────  │
    │  101         │ Raj       │ P3    │ 0         │ 2020-01-15     │ 2024-02-01│
    │  101         │ Raj       │ P4    │ 1         │ 2024-02-01     │ NULL     │
    │  102         │ Pri       │ P2    │ 1         │ 2019-06-10     │ NULL     │
    └─────────────────────────────────────────────────────────────────────────┘